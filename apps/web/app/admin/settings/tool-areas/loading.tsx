import { DirectoryPageSkeleton } from '@/components/loading/PageRouteSkeletons';

export default function LoadingToolAreas() {
  return <DirectoryPageSkeleton title="Tool areas" metrics={false} columns={4} filters={2} />;
}
